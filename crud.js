// create a file
const fs = require('fs');
const path= require('path');
const dirpath = path.join(__dirname,'curd');
const filePath = $

fs.writeFileSync(filePath,'this is simple file for just demo');
cos