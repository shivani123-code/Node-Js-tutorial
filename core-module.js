//console.log("code with shivani");

// with global core and create a file hello.txt file

/*const fs= require('fs');
fs.writeFileSync("hello.txt","code step by step");
fs.writeFileSync("hello2.txt","learn with code");

*/

// code with importing all file

const fs = require('fs').writeFileSync;
fs("abs.txt","hello");